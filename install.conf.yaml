- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.zshenv:
    ~/.zshrc:
    ~/.zpreztorc:
    ~/.zlogin:
    ~/.zlogout:
    ~/.zprezto: prezto/
    ~/.gitconfig:
    ~/.config/bat:
      create: true
      path: config/bat

- create:
    - ~/Work

- shell:
    - command: ./setup_homebrew.zsh
      stdout: true
      stderr: true
    - command: ./setup_zsh.zsh
      stdout: true
      stderr: true
    - command: ./setup_node.zsh
      stdout: true
      stderr: true
